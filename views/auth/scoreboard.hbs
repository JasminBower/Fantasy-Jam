<div class="body-scoreboard" >

<div>
<h2>Scoreboard</h2>
</div>
<br>
<div>
<h4>
  {{winner}}
  is the current leader!
</h4>
</div>
<br>
<div>
<table class="table table-dark table-striped">
  <thead>
    <tr>
      {{!-- <th scope="col">Classification</th> --}}
      <th scope="col">User</th>
      <th scope="col">Points</th>
    </tr>
  </thead>
  <tbody>
    {{#each sortedTeams}}
      <tr>
        {{!-- <th scope="row"></th> --}}
        <td>{{this.username}}</td>
        <td>{{this.teamScore}}</td>

      </tr>
    {{/each}}
  </tbody>
</table>
</div>
{{!-- <ol>
  {{#each sortedTeams}}
    <li>{{this.teamScore}}, {{this.username}}</li>
  {{/each}}
</ol> --}}
<br>

<br>
<div>
<h2>Racer Comments:</h2>

<form method="POST" action="/scoreboard">
  <div class="input-field">
    <textarea
      type="text"
      name="comment"
      rows="1"
      placeholder="What's on your mind?"
    ></textarea>
  </div>
  <input type="hidden" name="article-id" value="{{comment._id}}" />
  <button class="btn btn-primary" type="submit">Add Comment</button>
</form>

 {{!-- <ul>
 

<ul>
  {{#each mappedComments}}

    <li>{{this.comment}}</li>
    <p>by {{this.username}}</p>
    <p>at {{this.updatedAt}}</p>

    {{#if this.isCurrentUser}}
    <form method="GET" action="/comments/{{this._id}}">
    <button type="submit" class="btn btn-primary" value="{{this._id}}">Edit Comment</button>
    </form>


    <button type="submit" class="btn btn-primary">Delete Comment</button>


    {{/if}}

  {{/each}}
</ul> --}}
</div> 

<div>
<table class="table table-dark table-striped">
  <thead>
    <tr>
      {{!-- <th scope="col">Classification</th> --}}
      <th scope="col">User</th>
      <th scope="col">Comment</th>
      <th scope="col">Date</th>
       <th scope="col">Edit</th>
        <th scope="col">Delete</th>
    </tr>
  </thead>
  <tbody>
    {{#each mappedComments}}
      <tr>
        {{!-- <th scope="row"></th> --}}
        <td>{{this.username}}</td>
        <td>{{this.comment}}</td>
        <td>{{this.time}}</td>
        <td> {{#if this.isCurrentUser}}<form method="GET" action="/comments/{{this._id}}">
    <button type="submit" class="btn btn-primary" value="{{this._id}}">Edit Comment</button>
    </form>  {{/if}}</td>
        <td> {{#if this.isCurrentUser}}<form method="GET" action="/comments/{{this._id}}"><button type="submit" class="btn btn-primary">Delete Comment</button></form>  {{/if}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
</div>
<div>
<a href="../profile"><button class="btn btn-primary" type="submit" action="../profile">Go back to profile</button></a>
</div>



</div>